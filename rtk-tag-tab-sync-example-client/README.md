# rtk-tag-tab-sync-example

An example implementation of a multi tab front end developed with React and Redux Toolkit that uses rtk-tag-tab-sync to invalidate API tags in the Redux stores of each window.
